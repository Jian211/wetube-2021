extends base.pug

block contents 
    h1 Change title of video 
    form(method="POST")
        input(name="title", type="text", placeholder=video.title, required)
        input(type="submit", value="save")